Build the project and deploy to GitHub Pages.

Steps:
1. Run `npm run build` to build the production bundle
2. Run `npm run deploy` to deploy to GitHub Pages (this automatically runs build again via predeploy script)
3. Report the deployment status and any errors
